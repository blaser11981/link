# .env.example – copy to .env and fill real values

DB_HOST=localhost
DB_NAME=link
DB_USER=root
DB_PASS=

NOTION_TOKEN=secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional – change if not running on root
APP_URL=http://localhost

# Only needed later
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REDIRECT_URI=
MISSIVE_API_TOKEN=
